-# %h2= link_to '記録する', '/records/new'
- @records.each do |record|
  .record
    -# .record-edit-btn
    -#   = link_to record.content1, record_path(record.id), class: "record-edit-btn"
    .kakomi-tape1
      .record-content
        《問題点・解決したいこと》
        %br/
        = record.content1
      .record-content
        《調べたこと・仮説》
        %br/
        = record.content2
      .record-content
        《結果・学んだこと》
        %br/
        = record.content3
      .record-date
        記録日：#{record.created_at}
